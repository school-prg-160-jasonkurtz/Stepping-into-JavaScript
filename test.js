// Declares variable to store user's age.
var myAge = 26;

// Determines whether the user is the appropriate age or not.
if (myAge < 18 || myAge > 30 || myAge === 25) {
	document.write('you aint coming!');
} else {
	document.write('You can come, you cool dude');
}